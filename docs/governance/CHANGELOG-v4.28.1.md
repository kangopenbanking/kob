# Kang Open Banking API — v4.28.1

**Type:** Patch (additive only — Standing Order 4 Surgeon Rule, Standing Order 6 Version Gate)
**Date:** 2026-05-02

## Summary
Closes audit gap **G4 — SDK coverage metadata** by extending `info.x-sdk-libraries` with Java, Go, and Ruby entries. No existing field, schema, operation, or security scheme is modified.

## Changes
- `info.x-sdk-libraries.java` — typed client generated by `openapi-generator-cli` (okhttp-gson), version mirrors spec (4.28.1). Built by `.github/workflows/sdk-generate.yml`.
- `info.x-sdk-libraries.go` — published Go module `github.com/kangopenbanking/sdk-go` v1.5.0 (released alongside SDK 1.5.0 runbook).
- `info.x-sdk-libraries.ruby` — community implementation guide; consumers code-generate via `openapi-generator-cli` against the public `openapi.json`.

Each entry includes `status` (`generated` | `available` | `community-guide`), `repository`, `package_manager`, and `docs` URL so toolchains (Backstage, Stoplight, Redocly) can surface the SDKs automatically.

## G5 — Real-World examples
No spec change required. The route `/developer/examples/real-world` is already implemented in `src/pages/developer/RealWorldExamples.tsx` and registered in both `src/App.tsx` and `vite-plugin-prerender-docs.ts`. Resolves on next deploy.

## Compliance
- **Order 1 (Lock):** No renames or removals.
- **Order 2 (Ratchet):** Pure additions to `x-sdk-libraries`.
- **Order 3 (Audit Trail):** Cites OpenAPI 3.1.0 vendor-extension convention (`x-*`).
- **Order 4 (Surgeon):** Additive only.
- **Order 5 (Dead Code):** `x-sdk-libraries` is a documented vendor extension already referenced by the developer portal SDK page.
- **Order 6 (Version Gate):** 4.28.0 → 4.28.1 patch bump.
